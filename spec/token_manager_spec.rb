# frozen_string_literal: true

RSpec.describe TokenManager do
  it "has a version number" do
    expect(TokenManager::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
